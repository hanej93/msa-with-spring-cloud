server:
  port: 8888
spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///${user.home}\Desktop\Study\native-file-repo
        git:
          uri: https://github.com/hanej93/spring-cloud-config
          default-label: master
